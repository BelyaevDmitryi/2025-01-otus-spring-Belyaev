spring:
  datasource:
    url: jdbc:h2:mem:maindb
    username: SA
    password:
  sql:
    init:
      mode: always
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
server:
  port: 9090
